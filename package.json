{
  "name": "test_feed",
  "version": "0.0.0",
  "description": "Web app to show last 25 tweets in real-time.",
  "main": "js/app.js",
  "dependencies": {
    "bower": "^1.5.2"
  },
  "engines": {
    "node": "4.0.0"
  },
  "scripts": {
    "postinstall": "bower cache clean && bower install"
  }
}
